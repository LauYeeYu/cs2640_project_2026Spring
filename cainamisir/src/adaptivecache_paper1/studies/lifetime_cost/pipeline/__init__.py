"""Lifetime cost study pipeline.

Model-agnostic measurement of how much context compaction *actually* costs an
agent across its full trajectory, once prefix-cache invalidation is accounted
for. See ../README.md for the full design.
"""

__version__ = "0.1.0"

# Downloads
requests>=2.32

# Data analysis
pandas>=2.2
numpy>=1.26
scikit-learn>=1.6
pyarrow>=15.0
scipy>=1.12
xgboost>=2.0
datasets>=3.6

# Analysis plots
matplotlib>=3.8

# Agent harness — client library for vLLM's OpenAI-compatible API
# Points to localhost:8000, no OpenAI/Anthropic calls
openai>=1.0

# web_search tool uses Brave if BRAVE_API_KEY is set, else falls back to
# DuckDuckGo's keyless HTML endpoint (no extra deps, just requests+stdlib).

# SQL agent
duckdb>=0.10

# SWE-bench (bare execution; agent edits/tests inside per-task git workspace)
astropy>=6.0

# Model serving — install on GPU node with:
#   pip install "vllm==0.7.3"
# Pinned because 0.19.x wheels have a version-tag mismatch on PyPI that
# causes pip to fall back to a source build requiring nvcc.
# Note: vLLM 0.7.3 crashes on MIG slices because it tries to int()
# CUDA_VISIBLE_DEVICES (a MIG-<uuid>). agent/vllm_engine.py applies a
# monkey-patch automatically when it detects a MIG UUID.
#
# Tool-call parsing in agent/vllm_engine.py supports:
#   - Llama 3.1/3.3 JSON (with/without <|python_tag|> prefix)
#   - Qwen / Hermes-style <tool_call>...</tool_call> tags
vllm==0.7.3

huggingface_hub>=0.23
